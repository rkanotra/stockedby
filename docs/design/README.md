Design source of truth. Drop all Claude Design exports here (code + full-page
screenshots, mobile + desktop, per section). Current: hero export only —
remaining sections (Measure/Improve/Protect pillars, markets, compare, receipt
CTA) pending export. Load fonts from Google Fonts; never embed the woff2 from
these bundles.
